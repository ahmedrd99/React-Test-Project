import { useContext } from "react";

useContext
export   function UsernameDisplay({ username}) {
    
    return (
        <div>
            
           <span>{username} </span>
        </div>
    );
}